from flask import Blueprint

views = Blueprint('views', __name__) 
#variable name view can be anything
# this file is blueprint of our application which means their are some route here which we can organize in different files, names can be changed views in blueprint function can be anything else as well